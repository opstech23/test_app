<%= form_with(model: project_task, local: true) do |form| %>
  <% if project_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_task.errors.count, "error") %> prohibited this project_task from being saved:</h2>

      <ul>
      <% project_task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :task_title %>
    <%= form.text_field :task_title, id: :project_task_task_title %>
  </div>

  <div class="field">
    <%= form.label :task_description %>
    <%= form.text_area :task_description, id: :project_task_task_description %>
  </div>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id, id: :project_task_project_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :project_task_user_id %>
  </div>

  <div class="field">
    <%= form.label :approved_by_admin %>
    <%= form.check_box :approved_by_admin, id: :project_task_approved_by_admin %>
  </div>

  <div class="field">
    <%= form.label :approved_by_pto %>
    <%= form.check_box :approved_by_pto, id: :project_task_approved_by_pto %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
